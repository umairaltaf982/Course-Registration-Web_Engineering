<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <link rel="stylesheet" href="/css/student/student-dashboard.css">
</head>
<body>
    <div class="container">
        <h2>Welcome, <%= student.name %>!</h2>

        <h3>Your Registered Courses</h3>
        <ul>
            <% if (student.courses.length > 0) { %>
                <% student.courses.forEach(course => { %>
                    <li><%= course.name %> (<%= course.code %>)</li>
                <% }) %>
            <% } else { %>
                <p>You are not registered for any courses.</p>
            <% } %>
        </ul>

        <a href="/student/courses" class="btn">Register for Courses</a>
        <a href="/student/schedule" class="btn">View Weekly Schedule</a>
        <a href="/" class="btn">Logout</a>
    </div>
</body>
</html>
